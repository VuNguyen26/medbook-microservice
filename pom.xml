<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- 🏷️ Thông tin gốc của dự án -->
    <groupId>com.medbook</groupId>
    <artifactId>medbook-microservice</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>MedBook Microservice Parent</name>
    <description>Parent POM for MedBook Microservices</description>

    <!-- 🧩 Liệt kê tất cả các microservice con -->
    <modules>
        <module>discovery-service</module>
        <module>gateway-service</module>
        <module>user-service</module>
        <module>doctor-service</module>
        <module>appointment-service</module>
        <module>patient-service</module>
        <module>payment-service</module>
        <module>medicalrecord-service</module>
        <module>prescription-service</module>
        <module>notification-service</module> <!-- ✅ Đã thêm -->
    </modules>

    <!-- ⚙️ Cấu hình version dùng chung -->
    <properties>
        <java.version>21</java.version>
        <spring.boot.version>3.3.5</spring.boot.version> <!-- ✅ Đổi từ 3.5.6 -->
        <spring.cloud.version>2023.0.3</spring.cloud.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
    </properties>

    <!-- 📦 Quản lý version dùng chung cho toàn hệ thống -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- 📚 Dependencies dùng chung -->
    <dependencies>
        <!-- ✅ Dành cho test (nếu sau này cần) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- 🧰 Plugin chung -->
    <build>
        <pluginManagement>
            <plugins>

                <!-- ⚙️ Compiler plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>

                <!-- 🚀 Spring Boot plugin -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                </plugin>

                <!-- 🚫 Bỏ qua test khi build -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                    <configuration>
                        <skipTests>true</skipTests>
                    </configuration>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>

</project>
