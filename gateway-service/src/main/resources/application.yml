server:
  port: 8080

spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      routes:
        # 🧩 USER SERVICE
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=0   # Giữ nguyên /api/users/**

        # 🧩 DOCTOR SERVICE
        - id: doctor-service
          uri: lb://DOCTOR-SERVICE
          predicates:
            - Path=/api/doctors/**
          filters:
            - StripPrefix=0   # Giữ nguyên /api/doctors/**

        # 🧩 APPOINTMENT SERVICE
        - id: appointment-service
          uri: lb://APPOINTMENT-SERVICE
          predicates:
            - Path=/api/appointments/**
          filters:
            - StripPrefix=0   # Giữ nguyên /api/appointments/**

        # 🧩 PATIENT SERVICE
        - id: patient-service
          uri: lb://PATIENT-SERVICE
          predicates:
            - Path=/api/patients/**
          filters:
            - StripPrefix=0

        # 🧩 PAYMENT SERVICE
        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payments/**
          filters:
            - StripPrefix=0

        # 🧩 MEDICAL RECORD SERVICE
        - id: medicalrecord-service
          uri: lb://MEDICALRECORD-SERVICE
          predicates:
            - Path=/api/medical-records/**
          filters:
            - StripPrefix=0

        # 🧩 PRESCRIPTION SERVICE
        - id: prescription-service
          uri: lb://PRESCRIPTION-SERVICE
          predicates:
            - Path=/api/prescriptions/**
          filters:
            - StripPrefix=0

        # 🧩 NOTIFICATION SERVICE
        - id: notification-service
          uri: lb://NOTIFICATION-SERVICE
          predicates:
            - Path=/api/notifications/**
          filters:
            - StripPrefix=0

      # Cho phép tự động định tuyến (optional)
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      # 🧠 Cho phép FE (React, Angular, v.v.) truy cập CORS qua Gateway
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"

# 🛰️ Kết nối với Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

# 🩺 Actuator để kiểm tra health & route
management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always

# 🧾 Logging
logging:
  level:
    org.springframework.cloud.gateway: INFO
    reactor.netty.http.client: WARN
